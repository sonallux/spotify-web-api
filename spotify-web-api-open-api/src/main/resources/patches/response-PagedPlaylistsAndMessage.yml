description: Add PagedPlaylistsAndMessage response
operations:
  - op: test
    path: "$.components.responses.PagedPlaylistsAndMessage"
    value: null
  - op: put
    path: "$.components.responses"
    key: PagedPlaylistsAndMessage
    value:
      description: A paged set of playlists
      content:
        application/json:
          schema:
            type: object
            required:
              - playlists
              - message
            properties:
              message:
                type: string
                description: A description of the playlists
              playlists:
                $ref: '#/components/schemas/PlaylistsPagingObject'

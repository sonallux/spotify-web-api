description: Add PagedPlaylists response
operations:
  - op: test
    path: "$.components.responses.PagedPlaylists"
    value:
      description: A paged set of playlists
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/PagingPlaylistObject'
  - op: put
    path: "$.components.responses"
    key: PagedPlaylists
    value:
      description: A paged set of playlists
      content:
        application/json:
          schema:
            type: object
            required:
              - playlists
            properties:
              playlists:
                $ref: '#/components/schemas/PlaylistsPagingObject'
